// let inVar = 5;
// let div = 2;
// while (div < inVar) {
//   if (inVar % div === 0) {
//     console.log(`${inVar} is not prime`);
//   } else {
//     console.log(`${inVar} is prime`);
//     div = div + 1;
//   }
// }

//solution2:
let inputVar = 5;
let division = 2;
if (division < inputVar) {
  if (inputVar % division === 0) {
    console.log(`${inputVar} is not prime`);
  } else {
    console.log(`${inputVar} is prime`);
  }
}
